#version 430 core

layout (std430, binding = 0) buffer b_particles
{
  vec2 pos[3];
  vec2 vel[3];
};

layout (local_size_x = 3) in;

void main()
{
  float dt = 0.02;
  uint i = gl_GlobalInvocationID.x;
  pos[i] += vel[i] * dt;
}
